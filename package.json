{
  "name": "voha-frontendwebsite",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend for the Voha FastAPI server.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "python3 -m http.server ${PORT:-3000}",
    "preview": "npm run build && python3 -m http.server ${PORT:-4173} --directory dist",
    "start": "python3 -m http.server ${PORT:-3000}"
  }
}
